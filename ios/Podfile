# 1. 定义一个辅助方法来加载 Flutter 脚本
def load_flutter_podhelper
  podhelper_file = File.expand_path(File.join('Flutter', 'podhelper.rb'), __dir__)
  if File.exist?(podhelper_file)
    load podhelper_file
    return true
  end
  false
end

# 加载脚本（如果加载成功，它会提供 flutter_install_all_ios_pods 方法）
has_flutter_helper = load_flutter_podhelper

platform :ios, '13.0'

target 'Runner' do
  # 阿里 SDK 必须使用静态链接
  use_frameworks! :linkage => :static
  use_modular_headers!

  # 2. 如果脚本加载成功，安装所有插件
  if has_flutter_helper
    flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  end
end

post_install do |installer|
  # --- 补丁 A: 物理拷贝头文件（解决 Bridging Header 找不到文件的问题） ---
  puts "Checking for AlivcLivePusherPlugin.h..."
  # 在 Pods 目录下递归搜索
  found_h = Dir.glob("#{Dir.pwd}/Pods/**/AlivcLivePusherPlugin.h").first
  if found_h
    dest_h = File.join(Dir.pwd, 'Runner', 'AlivcLivePusherPlugin.h')
    puts "Copying header: #{found_h} -> #{dest_h}"
    require 'fileutils'
    FileUtils.cp(found_h, dest_h)
  end

  # --- 补丁 B: 修正构建设置 ---
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target) if defined?(flutter_additional_ios_build_settings)

    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
      config.build_settings['EXCLUDED_ARCHS[sdk=iphonesimulator*]'] = 'arm64'
      # 阿里 SDK 必备：允许非模块化引入
      config.build_settings['CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES'] = 'YES'

      if target.name == 'Runner'
        config.build_settings['SWIFT_OBJC_BRIDGING_HEADER'] = 'Runner/Runner-Bridging-Header.h'
        config.build_settings['HEADER_SEARCH_PATHS'] ||= '$(inherited) '
        config.build_settings['HEADER_SEARCH_PATHS'] << '"$(SRCROOT)/Runner" '
      end
    end
  end

  # --- 补丁 C: 强改 GeneratedPluginRegistrant.m ---
  registrant_m = File.join(Dir.pwd, 'Runner', 'GeneratedPluginRegistrant.m')
  if File.exist?(registrant_m)
    puts "Patching GeneratedPluginRegistrant.m..."
    content = File.read(registrant_m)
    # 将 @import 改为 #import "..."
    content.gsub!(/@import\s+flutter_livepush_plugin;/, '#import "AlivcLivePusherPlugin.h"')
    File.open(registrant_m, 'w') { |f| f.write(content) }
  end
end